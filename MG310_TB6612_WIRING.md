# MG310(1:20/13ppr) + TB6612(D153B) 接入 ELF-RV1126B：接线与实现

依据：底板原理图 `ELF-RV1126B V1.0.pdf` 第 3 页 "GPIO" 区（P1 排针）+ MG310 参数表 + D153B 原理图

## 1. 主板侧：P1 排针（2×25，2.54mm）

原理图中 P1 是唯一对外 GPIO 排针，实测各 GPIO5 网络均已加 ESD 保护（ESD5451N）。

| P1 引脚 | 网络 | GPIO 编号 | P1 引脚 | 网络 | GPIO 编号 |
| --- | --- | --- | --- | --- | --- |
| 31 | GPIO5_C1 | 177 | 32 | GPIO5_C0 | 176 |
| 33 | GPIO5_C3 | 179 | 34 | GPIO5_C2 | 178 |
| 35 | GPIO5_C5 | 181 | 36 | GPIO5_C4 | 180 |
| 37 | GPIO5_C7 | 183 | 38 | GPIO5_C6 | 182 |
| 41 | GPIO5_D1 | 185 | 42 | （空） | — |
| 43 | GPIO5_D0 | 184 | 44 | GPIO5_D2 | 186 |
| 45 | GPIO7_A0 | 224 | 46 | GPIO5_D3 | 187 |
| 47 | VCC_5V | — | 48 | VCC_5V | — |
| 49 | VCC_3V3 | — | 50 | VCC_3V3 | — |
| 39 | GND | — | 40 | GND | — |
| 29 / 19 / 9 / 1 | GND | — | 30 / 20 / 10 / 2 | GND | — |
| 3 | I2C4_SDA_M3 | — | 4 | I2C4_SCL_M3 | — |

> 编号换算：`GPIO n_xy = n×32 + (x−1)×8 + y`（A=1,B=2,C=3,D=4）

## 2. 接线总表（A 电机 + B 电机）

| TB6612(D153B) 丝印 | 接到 P1 引脚 | GPIO 编号 | 说明 |
| --- | --- | --- | --- |
| PWMA | 32 | 176 (GPIO5_C0) | A 电机速度 |
| AIN1 | 34 | 178 (GPIO5_C2) | A 方向 |
| AIN2 | 33 | 179 (GPIO5_C3) | A 方向 |
| PWMB | 31 | 177 (GPIO5_C1) | B 电机速度 |
| BIN1 | 36 | 180 (GPIO5_C4) | B 方向 |
| BIN2 | 35 | 181 (GPIO5_C5) | B 方向 |
| STBY | 模块 3V3 输出（默认）；可选 P1 GPIO | — | 高=工作；接 GPIO 可做软件急停（186/224） |
| E1A | 38 | 182 (GPIO5_C6) | A 编码器 A 相（**需降压**） |
| E1B | 37 | 183 (GPIO5_C7) | A 编码器 B 相（**需降压**） |
| E2A | 43 | 184 (GPIO5_D0) | B 编码器 A 相（**需降压**） |
| E2B | 41 | 185 (GPIO5_D1) | B 编码器 B 相（**需降压**） |
| GND1（逻辑地） | 40 或 39 | — | 必须共地 |

## 3. 电源设计（**电机功率全部来自外部 12V，不取板电**）

```
12V 适配器(+) ──► D153B 蓝色端子 VIN +   （即 VM，4.5~13.5V）
12V 适配器(−) ──► D153B 蓝色端子 VIN −   （即 GND2，功率地）
D153B 3V3 输出 ─► 编码器供电 + STBY 上拉   （模块自供电，均来自 12V）
P1 排针        ──► 只走控制信号（PWM/方向/编码器 AB）+ GND 共地，不取电
```

**与板端电源的隔离原则**：

| 项目 | 做法 |
| --- | --- |
| 电机功率（VM） | 仅外部 12V；板上 5V/3V3 **不参与**电机供电 |
| 编码器供电 | 用 D153B 的 3V3 输出（12V→模块 LDO），**不用 P1-49** |
| STBY 使能 | 用 D153B 3V3 上拉（默认）；可选接 P1 GPIO 做软件急停 |
| 信号地 | P1 GND 与模块 GND1 **必须共地**（仅信号回流，无功率电流） |
| 板端 5V/3V3 | 只供开发板自身与 USB 摄像头等外设，与电机系统隔离 |

- 12V 适配器选 **12V/3A** 以上：双电机额定 2×0.5A，单电机堵转 2A
- 电机 6P 排线**直插** D153B 的 Motor_A / Motor_B 座（模块内部把编码器电源/A/B 相引到 Output_IO）
- 上电顺序：先 12V、后开发板；断电相反；下电前先 `stop` 让 PWM=0

## 4. 编码器电平处理（**必做**）

MG310 数据手册明确："编码器带上拉输出，默认上拉到供电 VCC 引脚" → **输出高电平 = 编码器供电电压**。
而 D153B 的电机 6P 座给编码器供的是 **5V**；RV1126B GPIO 手册要求 **≤3.3V，严禁超压**。

两个方案（任选其一）：

**方案 A（推荐，无需额外元件）：把编码器供电改到 3V3**
1. 从 6P 电机线的插头中退出**红线**（编码器电源）：用细针挑开 PH2.0 端子卡扣即可抽出
2. 红线改接 D153B 的 3V3 输出（或 P1-49）
3. 其余 5 根线照常直插模块
4. 此时 E1A/E1B/E2A/E2B 输出≈3.3V，可直连 P1

**方案 B（不改线）：分压或电平转换**
- 每相串 2kΩ + 对地 3.3kΩ：5V × 3.3/(2+3.3) ≈ 3.1V（4 路共 8 个电阻）
- 或用 4 通道电平转换模块（TXS0108E）
- 即：模块 E1A/E1B/E2A/E2B → 转换 → P1-38/37/43/41

> 若暂不接编码器也能跑开环；但闭环/测速必须处理电平，否则可能损坏 GPIO。

## 5. 上电前检查清单

1. 万用表二极管档：电机线（黄/棕）之间有几十Ω~几Ω；编码器线之间不通
2. 12V 只进 VM/GND2，**不要**接到 5V/3V3 引脚
3. 用 12V 稳压源限流 0.5A 试机，防止接错烧模块
4. P1 排针上不要出现 5V 直接进 GPIO 的情况（先量 E1A/E1B 高点电平 ≤3.3V）

## 6. 已知限制：硬件 PWM 暂不可用

实测 `debug/pwm`：3 路已使能 PWM 全被内核占用（vdd-cpu / DSI 背光 / LCD 背光），其余 25 路设备树 disabled 且无 overlay 支持。
因此软件先用"软件 PWM（GPIO 位翻转）"或 PCA9685，最终建议改 DTS 使能 `pwm3`（M1 复用 → GPIO5_C0~C7，正是本方案用到的引脚）。

## 7. 功能实现（motor_ctl.py）

见工程根 `motor_ctl.py`：

```bash
sudo python3 motor_ctl.py --status              # 查看状态
sudo python3 motor_ctl.py A speed 100           # A 电机 100（-1000~1000，千分比）
sudo python3 motor_ctl.py A stop / brake        # 停止 / 刹车
sudo python3 motor_ctl.py A pos 360             # 位置模式：转 360 度
sudo python3 motor_ctl.py A rpm 40              # 闭环 40rpm（输出轴）
```

编码器换算（1:20.409、13ppr、4 倍频）：

- 输出轴每圈计数 = 13 × 20.409 × 4 ≈ **1061.3**
- 12V 空载电机轴约 800rpm → 输出轴 ≈ 40rpm → A 相频率 ≈ 175Hz，4 倍频 ≈ 700 计数/s（中断压力很小）